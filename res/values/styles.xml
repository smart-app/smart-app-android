<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="smart_tab_style">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">@dimen/smart_tab_bg_height</item>
        <item name="android:background">@color/smart_tab_bg</item>
    </style>

    <style name="smart_dialog_style" parent="android:Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <!--除去title-->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <!--除去背景色-->
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="smart_header_menu_style" parent="android:Animation">
        <item name="@android:windowEnterAnimation">@anim/smart_head_in</item>
        <item name="@android:windowExitAnimation">@anim/smart_head_out</item>
    </style>


    <style name="smart_dialog_loading" parent="@android:style/Theme.Dialog">
        <item name="android:windowIsFloating">true</item>     <!-- 设置未浮动窗口 -->
        <item name="android:windowFrame">@null</item>         <!-- 设置无边框 -->
        <item name="android:windowNoTitle">true</item>        <!-- 设置无标题 -->
        <item name="android:windowBackground">@color/transparent_background</item>  <!-- 设置完全透明 -->
        <item name="android:backgroundDimEnabled">true</item>                          <!-- 设置屏幕变暗 -->
    </style>
</resources>